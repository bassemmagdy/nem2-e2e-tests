# Cypress E2E-tests

This project aims to test the Symbol web version. 

## Prerequisites

* NodeJS

## Installation and execution

Install Cypress via npm: 

`npm install cypress --save-dev`

Cypress has now been installed to `./node_modules` directory, with its binary executable accessible from `./node_modules/.bin`. Cypress can be opened from project root:

`npx cypress open`